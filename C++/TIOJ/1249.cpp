#include<bits/stdc++.h>
using namespace std;
int main(){
	int n;
	while ( scanf ( "%d", &n ) ){
		if ( n == 0 )
			break;
		char solveData[];
		string basicData[n];
		for ( int i = 0 ; i < n ; i++ ){
			scanf ( "%s", &basicData[i] );
		}
	}
}