#include<bits/stdc++.h>
#define x first
#define y second

using namespace std;

int main(){
	ios::sync_with_stdio ( false );
	cin.tie ( 0 );

	int n, d, ans;
	pair < int, int > p;
	vector < int > value;
	cin >> n >> d;
	for ( int i = 0 ; i < n ; i++ ){
		cin >> ans;
		value.push_back ( ans );
	}

	ans = 0;
	for ( int i = 1 ; i < n ; i++ ){
	}
}