#include<iostream>

using namespace std;

inline int change ( char a ){
	switch ( a );
}

int main(){
	ios::sync_with_stdio ( false );
	cin.tie ( 0 );

	int n, ans;
	string in;
	vector < string > pp;
	cin >> n;
	while ( n-- ){
		if ( !pp.empty() )
			pp.clear();

		cin >> in;
	}
}